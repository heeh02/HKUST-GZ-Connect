# Campus Connect 2.0 implementation status

- Status: Current implementation index
- Owner: project maintainers
- Last verified: 2026-09-09
- Applies to: stable `v2.0.1` and `main@9e1135c05dc21998c66627e25477d4bd799cd5d7`
- Status authority: published stable tag and its exact build/source receipts
- Historical 2.0 product convergence: shipped; current maintenance/governance remain OPEN. Browser-based myPortal SMS MFA preserves its native child-window
  completion flow, while real Gateway MFA remains a separate evidence-driven capability
- Current maintenance authority: `main`; release evidence must reconcile the tag, build/source receipts and assets
- Active development authority: `main`; merged feature branches are not continuing authority
- Published stable line: `v2.0.1`
- Published Beta line: `v2.0.0-beta.1` at `f64f85d5756abc55bbab7a337f6212297d3c69da`
- Formal release version: `2.0.1`; designated-host artifacts are tied to the tag through the release receipts
- The stable release does not claim a real Gateway MFA provider or completion of M1–M5 modularization.

The current development queue includes an **unpublished 2.0.2 candidate**, not a newer stable
release. Its necessary release lane is #88/#95/#106/#107; modularization drafts #108–#120 are
separate. Updated dependencies and Desktop full tests have been verified on Mac and native 5070
Windows/Linux, but exact-main installer acceptance and release review are not yet complete.
The one-time maintainer-approved administrator exception only covers missing review/cloud checks;
it does not waive actual defects or change protections. No installed-App replacement was performed
during this acceptance round. Old candidate installers must not stand in for new exact-source
acceptance. See the historical
[integration/readiness snapshot](governance/2026-09-08-integration-readiness.md) for the PR chain,
remaining gates and the distinction between published, installed and reviewed-source states.

This file is the short current-state index. Product definitions and ADRs describe durable boundaries; phase
plans and engineering notes may retain historical sequencing. When they disagree about whether code is present,
the published release/tag and exact receipts, main at that commit, accepted contracts and then
historical audits apply in the [documentation authority order](README.md). Local worktrees cannot
promote an unmerged candidate to a published capability.

## Maintenance shipped in 2.0.1

- Windows login responsiveness: native private-file permission helper and validated display
  snapshots avoid repeated synchronous PowerShell startup without bypassing credential/write checks.
- Repository-ID-based update discovery is published, supporting the future owner transfer without
  trusting arbitrary redirects. Repository ownership has not changed yet.
- Four installers are published; original build source is `fafcb6c03c2d01eba2085c7d4a693e9f65ffc538`.
  Tag/main is `9e1135c05dc21998c66627e25477d4bd799cd5d7`. Only CI workflow and regression-test
  changes separate them; runtime, native source, dependencies and packaging inputs are identical.
- Windows/Linux acceptance was performed on the maintainer-designated 5070 host; Mac arm64/x64
  packages were built and verified. No macOS functional rerun is claimed by those release receipts.
- The [stable release](https://github.com/heeh02/HKUST-GZ-Connect/releases/tag/v2.0.1) includes
  `BUILD_RECEIPT.json`, `RELEASE_SOURCE_RECEIPT.json` and `SHA256SUMS.txt`. macOS is ad-hoc signed,
  not notarized; Windows is unsigned. The one-time administrator merge did not change protections
  or mark unexecuted GitHub checks successful.
- #88 ProxyCommand, #95 compatibility-default and the deferred Renderer/calendar/layout candidates
  are not part of this maintenance release. Their local validation is not release evidence.

## Implemented in 2.0.0

| Phase | Current production boundary |
| --- | --- |
| P1 | Manifest-bound reviewed HKUST(GZ) `SchoolProfile`, Engine config, branding and one resource source |
| P2 | Closed production provider composition; current capability remains password + Modern L3 |
| P3 | Profile/Account/Workspace persistence, journaled migration, bound credential envelope and recovery |
| P4 | One active Profile/Account/Engine, durable switch journal, cleanup barrier and controlled relaunch |
| P5 | Generation-bound Gateway connector consumed by Gateway HTTP and Modern transport before credentials |
| P6 implementation seam | Packaged 2.0.0 exposes reviewed HKUST(GZ) plus an unverified Other-school path with credential-free probe, credential gate, isolated provisioning and tombstone recovery |
| P7 implementation seam | Profile-bound Integration Center exposes one shared Clash/Mihomo YAML and a copy-only VS Code Remote-SSH snippet |
| Stabilization | Exact-tree source gates, authoritative status/ADR index, domain directories, dependency/cycle/unresolved-import gates and an empty root-debt ledger |
| P8 + product correction | Resource-first Student Home, 30 reviewed student-task entries, removable built-ins, favorites/recents/search, drag-and-drop personal groups, safe page capture, responsive layouts, ID-only open and on-demand Campus access |
| Phase 1 | Shared Design Tokens, restored Control Tower, balanced gold connection control, unique favorites/recent/recommended sections, real bilingual reviewed resources, structured notifications, grouped settings and an app-owned Browser Home |
| Phase 2 convergence | Packaged Other-school onboarding restored; reviewed registry/runtime/build/verifier no longer hardcode a single HKUST Profile or `hkustgz.json`; generic routing/PAC/health code carries no HKUST deployment defaults; the legacy HKUST Browser partition remains only at the bounded migration bridge; a non-public second reviewed fixture completed 12 isolated reviewed-to-reviewed switches |
| Phase 3 convergence | Stable recovery categories map failures to one bounded action; a sanitized CapabilitySnapshot is the sole Renderer capability source; Browser/PAC/Clash share one routing policy |
| Phase 4 convergence | Deterministic EasyConnect Gateway and binary diff receipts; capability/architecture changes require review; SOCKS/netstack performance gates and real Browser soak remain green |
| Update channel | Stable builds consult the stable GitHub Release channel; future prereleases remain explicit and cannot downgrade a stable installation |
| Upgrade compatibility | A checked-in 2.0 Workspace snapshot freezes all published HKUST(GZ) resource IDs and must retain custom URLs, favorites, recents, hidden built-ins and routing rules; CI also keeps the real two-process v1.2.3 migration green |
| 2.0 UI release | Restored the compact desktop shell, centered navigation, responsive card decks, paginated official applications and service desk, personal folders/favorites, weekly timetable and shared myPortal session reads. Login-time MFA uses a managed native child window so cross-origin cookies, `window.opener`, `postMessage` and self-close behavior remain intact. |

P7 uses redacted preview and explicit confirmation. The application has no install/update/remove action,
managed block, integration record store or third-party backup lifecycle. YAML save is a single user-selected,
owner-only atomic export with in-memory rollback.

## Current support statement

- HKUST(GZ) is the only packaged reviewed school Profile.
- `Other school` is visible in packaged 2.0 builds. It uses a credential-free public probe, one-use exact-origin
  confirmation, isolated storage and persistent unverified presentation; it does not by itself prove password
  auth, L3, Campus DNS, MFA or resource support.
- The repository supports multiple isolated Profile candidates and one active Profile at a time. It does not yet
  claim formally reviewed multi-school production compatibility.
- The production Gateway provider remains password + Modern L3. Generic MFA/control types and synthetic tests
  do not constitute a real school MFA provider.
- myPortal MFA is browser-owned: the user enters the SMS code manually in a managed child window; OTP values
  are neither autofilled from the campus password nor stored, logged or projected to Renderer state.

## Not implemented

| Phase | Missing production result |
| --- | --- |
| P9 | Any current-school evidence-backed Gateway MFA, session notice, password-expiry or multi-line provider |
| Formal multi-school | A second independently reviewed Profile with parity, canary, ownership and rollback evidence |

## Historical 2.0.0 evidence — not current runtime state

- The initial record used `415a3b5dfb9d11bffa14cde27e924af486823249` and CI runs `33864734522` /
  `33865449647`. It was superseded by the restored stable v2.0.0 tag at
  `5287c86842a506cd595e22b95d023b6a7e02cbea`; do not use the initial record as current artifact provenance.
- The stable Release contains exactly two DMGs, one Windows EXE and one Linux AppImage; GitHub
  publishes a SHA-256 digest for every asset.
- The published macOS applications passed `codesign --verify --deep --strict` with ad-hoc signing;
  they are not Developer ID notarized. The Windows installer is not Authenticode signed.
- `/Applications/hkustgzconnect.app` was replaced from the published Apple Silicon DMG, reports
  `2.0.0`, and retained the user-owned application data directory.
- The packaged arm64 Engine depends only on `/usr/lib/libiconv.2.dylib` and `/usr/lib/libSystem.B.dylib`.
- At the historical acceptance time, the installed 2.0.0 Engine ran from the verified App bundle
  with its loopback listener on `127.0.0.1:6180`; this is not a current process-status assertion.
- A SOCKS request to the school homepage returned HTTP 200. The HPC hostname resolved and completed HTTPS;
  the tested `/login` path returned HTTP 404 rather than a DNS/tunnel failure.
- A real installed Mihomo binary accepted the generated-compatible rule configuration and reached the school
  homepage through Mihomo → Campus Connect with HTTP 200; the disposable test configuration was removed.
- macOS, Windows and Linux release packages passed their content verifier and launch smoke gate on
  the exact tagged commit.
- Local canaries remain bounded staff evidence; publishing 2.0.0 does not constitute another
  school's compatibility proof.

## Deferred work after the 2.0.0 release

1. maintain the stable `2.0.x` line without expanding the product into SSH/HPC/Jupyter clients;
2. use a later patch line for subsequent maintenance rather than returning to the prerelease version;
3. keep the v1.2.3 migration fixture and three-platform package gates green for every update;
4. add a second reviewed Profile before claiming reviewed multi-school compatibility;
5. if the school later enables MFA on the VPN Gateway itself, open a separate task and implement it only from authorized,
   sanitized school evidence;
6. keep every later release bound to one exact tag, three-platform build and verified artifact set.

The application remains Web-resource-first for ordinary users and exposes external-tool configuration for
advanced users. It does not add an SSH client, HPC manager, Jupyter/database launcher or `ForwardLease`.
