# ADR-0005: External Tool Integration Center

- Status: Accepted; narrowed after local 2.0 product testing on 2026-08-26
- Decision owner: project maintainer
- Production surface: Control Tower, trusted IPC, and Main-owned export transactions

## Context

Campus Connect establishes an isolated campus connection and exposes an authenticated loopback SOCKS5
frontend. Ordinary users use Campus Browser and common websites. Advanced users may already own Clash,
Mihomo, VS Code, Remote-SSH, or another networking tool, but installing and maintaining those third-party
products is outside Campus Connect's responsibility.

Earlier P7 code explored managed Clash Verge Rev and OpenSSH installation plus PAC/manual exports. Product
testing showed that these controls were confusing, client-specific, and unnecessary. An advanced user can
install and configure their chosen tool; an ordinary user should not see installation controls at all.

## Decision

The production Integration Center only generates two non-destructive configuration artifacts:

```text
clash_mihomo_yaml   preview -> copy | save
vscode_remote_ssh   preview -> copy
```

Campus Connect never downloads, installs, launches, detects, or updates third-party software. It does not scan
the disk for configurations, modify a subscription, edit `~/.ssh/config`, install a VS Code extension, or claim
that another application imported the generated content.

Clash and Mihomo output contains one authenticated loopback SOCKS5 node and profile-derived routing rules. The
VS Code Remote-SSH output is a standard SSH host template containing the packaged `ProxyCommand`; the user
replaces `Host`, `HostName`, and `User` and pastes it into their own SSH configuration.

The application retains internal PAC generation where required for its own browser and routing implementation,
but PAC is not a public Integration Center artifact.

Website routing has one canonical `DomainRoutePolicyStore` snapshot. Campus Browser consumes that policy
automatically. Clash/Mihomo receives a point-in-time copy only after an explicit export and manual import;
ordinary browsers do not consume it automatically. The VS Code adapter is independent from website-domain
rules and generates only its Remote-SSH `ProxyCommand` template.

Adapter discovery is static and side-effect free. Listing adapters must not read credentials, build PAC,
canonicalize bookmark data, or hide every adapter because one export context is temporarily unavailable.
Adapter-specific Profile, listener, credential and policy validation begins only during `prepare`; confirmation
revalidates the same binding before any clipboard, file or sidecar effect.

Historical adapter IDs, managed coordinators, ownership records and disk-backup transactions are removed from
production. Ordinary YAML save uses a single-file atomic export with in-memory rollback only.

## Security and ownership

Renderer receives only a closed adapter view and redacted preview metadata. Generated bytes, local proxy
credentials, helper paths, account/workspace identities, and selected filesystem paths remain Main-owned.

Every export binds at least:

```text
Profile and Profile revision
Account and credential revision
Workspace epoch
listener host, port, and authentication policy
local credential reference
network-rules digest
```

Copy and save are two-step operations: Main prepares exact bytes and a short-lived opaque confirmation handle;
confirmation revalidates the current binding before writing to the clipboard or to the one path explicitly
selected by the user. Save uses owner-only atomic output and readback validation.

Clash/Mihomo configuration contains a local proxy credential. It is not the school VPN password, but it can use
the active campus tunnel and therefore must not be uploaded, synchronized or shared.

The VS Code snippet does not embed that credential. It references an owner-only sidecar which is created by Main
only when the user confirms copying the snippet. The packaged helper checks the active profile before opening a
connection, so a host configuration for one school cannot silently use another school's active tunnel.

## Product boundary

The Center is not an installer or resource launcher. It does not implement:

- a built-in SSH client, terminal, HPC/Jupyter/database manager, or `ForwardLease`;
- Clash Verge Rev installation or managed extension lifecycle;
- managed OpenSSH `Include` or automatic SSH config editing;
- PAC/manual endpoint export in the production UI;
- arbitrary executable launch, extension installation, subscription mutation, or third-party discovery;
- any weakening of loopback-only listening or local proxy authentication.

Usage guidance belongs in README. Support for another configuration format requires a separately reviewed,
copy/save-only adapter with bounded generation, validation, redacted preview, and current Profile/Account
binding.

## Verification requirements

- production list contains exactly `clash_mihomo_yaml` and `vscode_remote_ssh`;
- trusted IPC rejects every historical or unknown adapter ID;
- VS Code supports copy only; no adapter exposes install/update/remove;
- Renderer never receives generated payloads, credentials, helper paths, or target paths;
- binding drift invalidates a pending handle before clipboard or filesystem mutation;
- Clash/Mihomo output validates as bounded UTF-8 and includes no school VPN credential;
- VS Code output validates as one profile-bound `ProxyCommand` template;
- third-party managed coordinators are absent from the production runtime composition.
