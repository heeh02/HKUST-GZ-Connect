# 2.0.2 exact-candidate acceptance checkpoint

- Status: Historical acceptance receipt; release gate incomplete
- Owner: Desktop / release maintainers, PR #106
- Verified: 2026-09-09
- Main baseline: `9e1135c05dc21998c66627e25477d4bd799cd5d7`
- Tested candidate: `7699db0a361239de5804fd2410d678deac7890ca`
- Published stable release at readback: `v2.0.1`; no 2.0.2 Release/draft assets found

## Scope separation

This is the actual 2.0.2 calendar/category candidate, not the later Renderer/Engine governance
stack. PR #107 Windows fixture repairs and PR #108 onward modularization are not merged into this
tree. The prior 74-failure review-chain Windows result cannot be reused as this candidate's result.
The latest installed-App fixture addition changes tests only; candidate runtime is unchanged from
PR #106's preceding cache-revocation checkpoint. This run tests source assets, not the installed App.

## Native evidence

All hosts checked out the same candidate using Git bundles with available main prerequisites.
Mac used Node 24.19; designated 5070 Windows and Linux used Node 24.20. Linux used `umask 022`.
Existing dependency directories were linked rather than downloaded or duplicated.

| Check | Result |
| --- | --- |
| Mac full `node --test` | 1,284 total; 1,278 passed; 6 platform skips; 0 failures |
| 5070 Linux full `node --test` | Same counts as Mac |
| 5070 native Windows full `node --test` | 1,284 total; 1,168 passed; 40 skips; **76 failures** |
| Schedule navigation Electron fixture | Passed on Mac, Windows and Linux/Xvfb |
| Personal category expansion Electron fixture | Passed on all three platforms |
| Control-shell layout Electron fixture | Passed on all three platforms |
| Architecture / syntax / secret / install-script / governance | Passed on Mac; syntax covered 469 files |

Run from `desktop/`:

```sh
node --test
electron e2e/schedule-navigation.electron.js
electron e2e/category-expand.electron.js
electron e2e/control-shell-layout.electron.js
```

Linux runs each Electron fixture under `xvfb-run -a`. Windows runs the shared Electron CLI using
the explicit Node 24 executable. Schedule checks cover initial/uncached pending grid, arbitrary
date, adjacent weeks/today, cached refresh, refresh failure, detail centering, keyboard, precise
geometry/overlap, narrow/wide/zoom and authentication-expiry removal/recovery. Category checks cover
small-list expansion, all rows, Escape, responsive spreading and reduced motion. APIs are synthetic;
these are not live-school or installer/signature results. Windows continues to log GPU exit-code-34
warnings; passing DOM assertions do not establish clean hardware acceleration.

The first Windows full run had 78 failures and only 1,261 discovered tests: two suites failed to
load `electron` / `@electron/asar` before the dependency link existed. After resolving those exact
environment failures, the full result above is authoritative for this candidate. No test or
production policy was skipped/relaxed. Native helper source SHA-256 was verified as
`9341aba8971b5b45b73adad9a7eb48c505559214d4f14ad5b1d3ae9f6d0e8af8` before reusing test helper
`43448327e29d9054cdd65d2c822d216d880de922a97c72e71d17e15c4b0f5d9c`.

## Windows attribution still required

The 76 failures group by test ownership, **not by proven root cause**:

| Test domain | Failures |
| --- | ---: |
| Persistence / credentials / migrations | 46 |
| Profiles / onboarding / provisioning | 11 |
| Active-context switching | 5 |
| Engine connection | 3 |
| Root legacy suites | 3 |
| Card Board | 2 |
| Platform private-file | 2 |
| Resource favorites | 2 |
| Integration adapters | 1 |
| Integration IPC suite | 1 |

Some failure locations are already covered by separately reviewed fixture repairs (including
PR #107); others contain native-path assumptions. This classification does not prove all are
fixture-only, pre-existing, unrelated to 2.0.2, or safe to waive. Preserve ownership/ACL/no-follow
checks and attribute each remaining failure before claiming Windows acceptance.

Local full logs: `/tmp/hkustgz-202-candidate-mac-full-20260909.log` on Mac;
`/home/owner/work/hkustgz-202-candidate-full-20260909.log` on 5070 Linux;
`C:\Users\RBMS\work\hkustgz-202-candidate-deps-full-20260909.log` on 5070 Windows.
Logs are test evidence, not distributable release artifacts.

## Remaining release gates / rollback

1. Attribute and resolve Windows native failures on this final candidate; do not import the entire
   optional modularization stack to get unrelated fixes.
2. Rebuild and verify exact-source macOS arm64/x64, Windows x64 and Linux x64 artifacts. Earlier
   packages from `b3cdc0a` lack subsequent runtime fixes and are not eligible for publication.
3. Record one-source build receipts, digests and actual signing/notarization state.
4. Obtain separate maintainer authorization for merge/publication. Main protections and required
   checks remain unchanged; local evidence does not impersonate Actions or independent review.

No new installer, installed-App replacement, Actions build, merge, tag, release, repository transfer
or protection change was performed. No user data or real school credentials were accessed. This
checkpoint adds acceptance evidence only; reverting it has no runtime or stored-data effect.
